@page "/viewRecipes"
@using Model
@using Blazor.Data
@inject IRecipeService RecipeService
@inject NavigationManager NavigationManager
<h2 class="title">ViewRecipes</h2>

<div class="row">
    @foreach (var recipe in Recipes) {
        <div class="col-sm-3">
            <div class="card" style="width: 18rem;">
                <img src="../Images/@recipe.name" class="card-img-top" alt="arrabiata">
                <div class="card-body">
                    <h5 class="card-title">@recipe.name</h5>
                    <p class="card-text">@recipe.instructions</p>
                    <button class="btn btn-primary" @onclick="@(() => NavigateToComponent(recipe))">Check the recipe now!</button>
                </div>
            </div>
        </div>
    }
</div>

@code {
<<<<<<< HEAD
  public IList<Recipe> Recipes { get; set; }
  

  protected override async Task OnInitializedAsync()
  {
    Recipes = await RecipeService.GetRecipesAsync();
    Console.WriteLine(Recipes);
    
  }

  private void NavigateToComponent(Recipe p) {
    NavigationManager.NavigateTo("order/" + p.name);
  }
=======
    public IList<Recipe> Recipes { get; set; }

    protected override async Task OnInitializedAsync()
    {
      Recipes = await RecipeService.GetRecipesAsync();
    }

    private void NavigateToComponent(Recipe p) {
      NavigationManager.NavigateTo("recipeView/" + p.name);
    }
>>>>>>> 6a52e9e35887e1b725388569f8bc930779bf2f3f
}